linecontent = {ASCII_ALPHANUMERIC*}
line = _{"\"" ~ linecontent ~ "\"" ~ NEWLINE}

msgid = {"msgid " ~ (line)+}
msgstr = {"msgstr " ~ (line)+}

entry = {msgid ~ msgstr}

file = {(SOI ~ (entry ~ NEWLINE*)+ ~ EOI)}
WHITESPACE = _{ " " }
